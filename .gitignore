# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Environment & secrets
.env
.env.*
!.env.example

# Node
node_modules/
*.log

# Bot state (runtime, not source)
farcaster-bot/*-state.json
farcaster-bot/state.json
farcaster-bot/state.json.bak
farcaster-bot/deploy-queue.json
farcaster-bot/deploy-map.json
farcaster-bot/liquidat*
farcaster-bot/*-report-*.json

# MCP server tokens
mcp-server/.mcpregistry_*

# External repos
clanker-v4-reference/
pumpclaw-action/

# Internal operational scripts (not part of public repo)
farcaster-bot/
tools/

# Compiled output (build locally before npm publish, don't commit)
client-cli/dist/
mcp-server/dist/
elizaos-plugin/dist/
farcaster-bot/dist/

# Shared lib compiled output
shared/*.js
shared/*.d.ts

# Misc
outreach_log.txt
client-web/dist/
memory/
